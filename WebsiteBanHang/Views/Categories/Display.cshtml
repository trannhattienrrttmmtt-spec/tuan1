@model WebsiteBanHang.Models.Category

@{
    ViewData["Title"] = "Chi tiết danh mục";
}

<section class="container page-section">
    <div class="checkout-card">
        <span class="eyebrow">Category</span>
        <h2>@Model.Name</h2>
        <p class="text-muted">Mã danh mục: #@Model.Id</p>

        <a asp-action="Update" asp-route-id="@Model.Id" class="btn btn-warning">Chỉnh sửa</a>
        <a asp-action="Index" class="btn btn-outline-dark">Quay lại</a>
    </div>
</section>
